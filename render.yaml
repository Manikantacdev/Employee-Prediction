services:
  - type: web
    name: employee-prediction
    env: python
    plan: free
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: cd Flask && python production_server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
